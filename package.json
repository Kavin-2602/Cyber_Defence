{
    "name": "defence-cyber-portal-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm install --prefix defence-frontend && npm run build --prefix defence-frontend",
        "install": "npm install --prefix defence-frontend"
    }
}